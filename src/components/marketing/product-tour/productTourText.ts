/**
 * Product Tour marketing typography — dark neutral body copy.
 * Scoped to Product Tour components only (does not change global tokens).
 */

/** Primary body / supporting paragraphs (#111827 — editorial neutral). */
export const tourTextBody = "text-[#111827]";

/** Small metadata labels (section chrome, inactive step labels). */
export const tourTextMeta = "text-black/70";

/** Intentional text links and primary interactive controls. */
export const tourTextLink = "text-brand-blue";

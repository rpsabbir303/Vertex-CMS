/**
 * Showcase mockup slots — swap these with final screenshot paths later.
 *
 * Place files in /public and set paths like:
 *   mockup01: "/showcase/mockup01.png"
 *   mockup02: "/showcase/mockup02.png"
 *   mockup03: "/showcase/mockup03.png"
 *   mockup04: "/showcase/mockup04.png"
 *
 * Leave as null to use the built-in HTML/CSS product mockups.
 */
export const showcaseSlots: {
  mockup01: string | null;
  mockup02: string | null;
  mockup03: string | null;
  mockup04: string | null;
} = {
  mockup01: null,
  mockup02: null,
  mockup03: null,
  mockup04: null,
};

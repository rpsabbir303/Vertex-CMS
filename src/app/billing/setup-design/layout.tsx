import "@/app/billing/billing-setup.css";

export default function BillingSetupDesignLayout({ children }: { children: React.ReactNode }) {
  return <div className="billing-font-scope font-sans antialiased">{children}</div>;
}
